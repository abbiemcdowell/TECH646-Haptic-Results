conda install pytorch torchvision torchaudio pytorch-cuda -c pytorch -c nvidia (don't trust it being 12.1)
conda install numpy=1.25 scikit-learn=1.3 pandas matplotlib

conda clean --all --yes

which python